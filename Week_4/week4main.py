def main():
    variable_one = getInput(getCardNumber)
    card_type_variable = cardNumberType()
    valid_variable = isCardValid()
    # which calls
    sumOfDigits(cardNumber)
    # which calls cardType
    # and doubleDigitValue


main()
